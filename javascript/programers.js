function solution(array, n) {
    let empty = 0;
    for (a of array) {
        console.log(a);
        // if(a === n) {
        //  empty += 1; 
        // }
 };
}

let A = [10, 10, 20, 30, 40, 50]
let B = [1, 1, 2, 2, 3, 4, 5]
solution(A, 10);
